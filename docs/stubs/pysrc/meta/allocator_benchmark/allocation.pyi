from typing import Any

"""
Long/flat allocation bridge and forward utility accounting for W2.
"""

ALLOCATED_DECISION_COLUMNS: tuple[str, ...] = ...
UTILITY_DECISION_COLUMNS: tuple[str, ...] = ...
def apply_long_flat_allocation_bridge(instrument_scores: pd.DataFrame, config: W2AllocatorBenchmarkConfig) -> pd.DataFrame: ...
def compute_turnover_and_net_utility(instrument_decisions: pd.DataFrame, config: W2AllocatorBenchmarkConfig) -> pd.DataFrame: ...
